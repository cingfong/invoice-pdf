export interface formData {
  name: string | null;
  spec: string | null;
  number: number | null;
  unit: string | null;
  price: number | null;
  itemTotal: number | null;
  remark: string | null;
}